{
  "name": "bothy-blend",
  "version": "1.0.0",
  "private": true,
  "description": "Bothy Blend",
  "keywords": [
    "gatsby"
  ],
  "scripts": {
    "build": "gatsby build --prefix-paths",
    "develop": "gatsby develop --host=0.0.0.0",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,md}\"",
    "clean": "gatsby clean"
  },
  "dependencies": {
    "@fontsource/roboto": "^4.2.2",
    "gatsby": "^3.0.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "prettier": "2.2.1"
  }
}
